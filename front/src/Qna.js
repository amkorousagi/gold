import Header from "./Header"
import Divider from "@material-ui/core/Divider"
const Qna = ({ match, location }) => {
  return (
    <div>
      <Header />
      <Divider />

    </div>
  )
}

export default Qna
//crud, page..